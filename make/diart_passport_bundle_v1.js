/**
 * DiArt Passport Make Bundle
 * File: make/diart_passport_bundle_v2.js
 * Version: 2.0.0-approved
 *
 * Approved two-page Passport runtime for Make.
 */

"use strict";

const BUNDLE_VERSION = "2.0.0-approved";
const EMBEDDED_SOURCES = {"passport/layout.js": "/**\n * DiArt Color Passport\n * File: passport/layout.js\n * Version: 3.0.0-approved\n *\n * Fixed geometry for the approved DiArt Color Passport mockup.\n * One page: 768 × 1024 px.\n */\n\n\"use strict\";\n\nconst LAYOUT_VERSION = \"3.0.0-approved\";\n\nconst PAGE = Object.freeze({\n  width: 768,\n  height: 1024,\n  viewBox: \"0 0 768 1024\",\n  outerRadius: 18,\n  margin: 4\n});\n\nconst TOKENS = Object.freeze({\n  pageBackground: \"#FBF7F1\",\n  panelBackground: \"#FFFDFC\",\n  panelSoft: \"#F8F0E8\",\n  ink: \"#24160F\",\n  muted: \"#6C5B50\",\n  line: \"#E7DDD3\",\n  accentFallback: \"#7B3F20\",\n  white: \"#FFFFFF\",\n  panelRadius: 16,\n  cardRadius: 10,\n  thinStroke: 1,\n  mediumStroke: 1.4\n});\n\nconst PAGE_1 = Object.freeze({\n  logo: Object.freeze({ x: 28, y: 24, width: 204, height: 146 }),\n\n  identity: Object.freeze({\n    x: 245, y: 28, width: 370, height: 154,\n    eyebrowY: 48,\n    titleRuY: 92,\n    titleEnY: 126,\n    descriptionY: 151,\n    descriptionLineHeight: 20\n  }),\n\n  confidence: Object.freeze({\n    x: 620, y: 28, width: 132, height: 154,\n    labelY: 49,\n    percentY: 106,\n    starsY: 142,\n    pillX: 632,\n    pillY: 153,\n    pillWidth: 108,\n    pillHeight: 24\n  }),\n\n  portrait: Object.freeze({\n    x: 28, y: 194, width: 302, height: 334, radius: 18\n  }),\n\n  scalesPanel: Object.freeze({\n    x: 344, y: 194, width: 408, height: 334, radius: 18,\n    rowHeight: 83.5,\n    iconX: 374,\n    titleX: 420,\n    labelsX: 546,\n    scaleX1: 596,\n    scaleX2: 724,\n    rows: Object.freeze({\n      temperature: Object.freeze({ top: 208 }),\n      depth: Object.freeze({ top: 291 }),\n      contrast: Object.freeze({ top: 374 }),\n      clarity: Object.freeze({ top: 457 })\n    })\n  }),\n\n  naturalPalette: Object.freeze({\n    x: 28, y: 542, width: 338, height: 298, radius: 18,\n    titleX: 52,\n    titleY: 574,\n    itemY: 603,\n    itemWidth: 100,\n    itemGap: 5,\n    columns: Object.freeze([\n      Object.freeze({ x: 40, centerX: 90 }),\n      Object.freeze({ x: 145, centerX: 195 }),\n      Object.freeze({ x: 250, centerX: 300 })\n    ]),\n    cropSize: 88,\n    cropY: 620,\n    swatchY: 720,\n    swatchSize: 13,\n    swatchGap: 5,\n    nameY: 758,\n    hexY: 805\n  }),\n\n  nearestSeasons: Object.freeze({\n    x: 378, y: 542, width: 374, height: 298, radius: 18,\n    titleX: 468,\n    titleY: 574,\n    donutCenterX: 486,\n    donutCenterY: 676,\n    donutOuterRadius: 93,\n    donutInnerRadius: 48,\n    listX: 594,\n    listY: 614,\n    listRowHeight: 59,\n    pinSize: 36,\n    noteX: 415,\n    noteY: 814,\n    noteWidth: 302\n  }),\n\n  meta: Object.freeze({\n    x: 28, y: 858, width: 724, height: 88, radius: 14,\n    columns: Object.freeze([\n      Object.freeze({ x: 56, width: 206 }),\n      Object.freeze({ x: 281, width: 206 }),\n      Object.freeze({ x: 506, width: 206 })\n    ]),\n    iconY: 878,\n    labelY: 891,\n    valueY: 917,\n    divider1X: 268,\n    divider2X: 493\n  }),\n\n  footer: Object.freeze({\n    x: 28, y: 956, width: 724, height: 60,\n    logoX: 298,\n    logoY: 942,\n    logoWidth: 172,\n    logoHeight: 54,\n    sloganY: 1006,\n    leftStarX: 276,\n    rightStarX: 492\n  })\n});\n\nconst PAGE_2 = Object.freeze({\n  header: Object.freeze({\n    titleX: 384,\n    titleY: 47,\n    leftStarX: 168,\n    rightStarX: 600\n  }),\n\n  palette12: Object.freeze({\n    x: 28, y: 76, width: 712, height: 385, radius: 16,\n    titleY: 102,\n    gridX: 42,\n    gridY: 118,\n    columns: 6,\n    rows: 2,\n    cardWidth: 103,\n    cardHeight: 154,\n    gapX: 10,\n    gapY: 18,\n    colorHeight: 78,\n    nameY: 102,\n    hexY: 137\n  }),\n\n  neutral: Object.freeze({\n    x: 28, y: 474, width: 350, height: 195, radius: 16,\n    titleY: 505,\n    swatchX: 45,\n    swatchY: 526,\n    swatchWidth: 45,\n    swatchHeight: 76,\n    gap: 9,\n    nameY: 619,\n    hexY: 647\n  }),\n\n  accent: Object.freeze({\n    x: 390, y: 474, width: 350, height: 195, radius: 16,\n    titleY: 505,\n    swatchX: 407,\n    swatchY: 526,\n    swatchWidth: 45,\n    swatchHeight: 76,\n    gap: 9,\n    nameY: 619,\n    hexY: 647\n  }),\n\n  usage: Object.freeze({\n    x: 28, y: 686, width: 350, height: 235, radius: 16,\n    titleX: 86,\n    titleY: 716,\n    iconX: 48,\n    textX: 88,\n    rowY: 750,\n    rowHeight: 45,\n    iconSize: 28\n  }),\n\n  important: Object.freeze({\n    x: 390, y: 686, width: 350, height: 235, radius: 16,\n    starX: 426,\n    starY: 728,\n    titleX: 463,\n    titleY: 735,\n    textX: 414,\n    textY: 778,\n    textWidth: 178,\n    lineHeight: 22,\n    branchX: 590,\n    branchY: 705,\n    branchWidth: 130,\n    branchHeight: 196\n  }),\n\n  footer: Object.freeze({\n    x: 28, y: 938, width: 712, height: 78,\n    logoX: 298,\n    logoY: 922,\n    logoWidth: 172,\n    logoHeight: 54,\n    sloganY: 1007,\n    leftStarX: 276,\n    rightStarX: 492\n  })\n});\n\nfunction validateBox(name, box, errors) {\n  if (\n    typeof box.x !== \"number\" ||\n    typeof box.y !== \"number\" ||\n    typeof box.width !== \"number\" ||\n    typeof box.height !== \"number\"\n  ) {\n    errors.push(`${name}: invalid box.`);\n    return;\n  }\n\n  if (\n    box.x < 0 ||\n    box.y < 0 ||\n    box.x + box.width > PAGE.width ||\n    box.y + box.height > PAGE.height\n  ) {\n    errors.push(`${name}: outside page bounds.`);\n  }\n}\n\nfunction validateLayout() {\n  const errors = [];\n\n  [\n    [\"PAGE_1.portrait\", PAGE_1.portrait],\n    [\"PAGE_1.scalesPanel\", PAGE_1.scalesPanel],\n    [\"PAGE_1.naturalPalette\", PAGE_1.naturalPalette],\n    [\"PAGE_1.nearestSeasons\", PAGE_1.nearestSeasons],\n    [\"PAGE_1.meta\", PAGE_1.meta],\n    [\"PAGE_1.footer\", PAGE_1.footer],\n    [\"PAGE_2.palette12\", PAGE_2.palette12],\n    [\"PAGE_2.neutral\", PAGE_2.neutral],\n    [\"PAGE_2.accent\", PAGE_2.accent],\n    [\"PAGE_2.usage\", PAGE_2.usage],\n    [\"PAGE_2.important\", PAGE_2.important],\n    [\"PAGE_2.footer\", PAGE_2.footer]\n  ].forEach(([name, box]) => validateBox(name, box, errors));\n\n  return {\n    ok: errors.length === 0,\n    errors\n  };\n}\n\nmodule.exports = {\n  LAYOUT_VERSION,\n  PAGE,\n  TOKENS,\n  PAGE_1,\n  PAGE_2,\n  validateLayout\n};\n", "passport/svg.js": "/**\n * DiArt Passport\n * File: passport/svg.js\n * Version: 2.0.0\n *\n * Shared SVG helpers for both passport pages.\n */\n\n\"use strict\";\n\nconst SVG_VERSION = \"2.0.0\";\nconst SVG_NAMESPACE = \"http://www.w3.org/2000/svg\";\n\nfunction esc(value) {\n  return String(value ?? \"\")\n    .replace(/&/g, \"&amp;\")\n    .replace(/</g, \"&lt;\")\n    .replace(/>/g, \"&gt;\")\n    .replace(/\"/g, \"&quot;\")\n    .replace(/'/g, \"&apos;\");\n}\n\nfunction attrs(attributes = {}) {\n  return Object.entries(attributes)\n    .filter(([, value]) =>\n      value !== undefined &&\n      value !== null &&\n      value !== false &&\n      value !== \"\"\n    )\n    .map(([key, value]) => `${key}=\"${esc(value)}\"`)\n    .join(\" \");\n}\n\nfunction svg(width, height, viewBox) {\n  return `<svg xmlns=\"${SVG_NAMESPACE}\" width=\"${width}\" height=\"${height}\" viewBox=\"${esc(viewBox)}\">`;\n}\n\nfunction end() {\n  return \"</svg>\";\n}\n\nfunction defs(content = \"\") {\n  return `<defs>${content}</defs>`;\n}\n\nfunction group(content = \"\", attributes = {}) {\n  return `<g ${attrs(attributes)}>${content}</g>`;\n}\n\nfunction rect({\n  x,\n  y,\n  width,\n  height,\n  rx = 0,\n  ry = null,\n  fill = \"none\",\n  stroke = null,\n  strokeWidth = null,\n  opacity = null,\n  filter = null,\n  id = null\n}) {\n  return `<rect ${attrs({\n    id,\n    x,\n    y,\n    width,\n    height,\n    rx,\n    ry: ry ?? rx,\n    fill,\n    stroke,\n    \"stroke-width\": strokeWidth,\n    opacity,\n    filter\n  })}/>`;\n}\n\nfunction circle({\n  cx,\n  cy,\n  r,\n  fill = \"none\",\n  stroke = null,\n  strokeWidth = null,\n  opacity = null,\n  filter = null,\n  id = null\n}) {\n  return `<circle ${attrs({\n    id,\n    cx,\n    cy,\n    r,\n    fill,\n    stroke,\n    \"stroke-width\": strokeWidth,\n    opacity,\n    filter\n  })}/>`;\n}\n\nfunction line({\n  x1,\n  y1,\n  x2,\n  y2,\n  stroke = \"#000000\",\n  strokeWidth = 1,\n  linecap = null,\n  dasharray = null,\n  opacity = null\n}) {\n  return `<line ${attrs({\n    x1,\n    y1,\n    x2,\n    y2,\n    stroke,\n    \"stroke-width\": strokeWidth,\n    \"stroke-linecap\": linecap,\n    \"stroke-dasharray\": dasharray,\n    opacity\n  })}/>`;\n}\n\nfunction image({\n  href,\n  x,\n  y,\n  width,\n  height,\n  preserveAspectRatio = \"xMidYMid slice\",\n  clipPath = null,\n  opacity = null\n}) {\n  if (!href) return \"\";\n\n  return `<image ${attrs({\n    href,\n    x,\n    y,\n    width,\n    height,\n    preserveAspectRatio,\n    \"clip-path\": clipPath,\n    opacity\n  })}/>`;\n}\n\nfunction text({\n  value,\n  x,\n  y,\n  fontFamily = \"Arial, Helvetica, sans-serif\",\n  fontSize = 24,\n  fontWeight = 400,\n  fill = \"#241912\",\n  textAnchor = \"start\",\n  letterSpacing = 0,\n  fontStyle = null,\n  opacity = null\n}) {\n  return `<text ${attrs({\n    x,\n    y,\n    \"font-family\": fontFamily,\n    \"font-size\": fontSize,\n    \"font-weight\": fontWeight,\n    fill,\n    \"text-anchor\": textAnchor,\n    \"letter-spacing\": letterSpacing,\n    \"font-style\": fontStyle,\n    opacity\n  })}>${esc(value)}</text>`;\n}\n\nfunction path({\n  d,\n  fill = \"none\",\n  stroke = null,\n  strokeWidth = null,\n  linecap = null,\n  linejoin = null,\n  opacity = null,\n  filter = null\n}) {\n  return `<path ${attrs({\n    d,\n    fill,\n    stroke,\n    \"stroke-width\": strokeWidth,\n    \"stroke-linecap\": linecap,\n    \"stroke-linejoin\": linejoin,\n    opacity,\n    filter\n  })}/>`;\n}\n\nfunction clipPath(id, content) {\n  return `<clipPath id=\"${esc(id)}\">${content}</clipPath>`;\n}\n\nfunction shadowFilter(\n  id = \"shadow\",\n  {\n    dx = 0,\n    dy = 8,\n    stdDeviation = 12,\n    floodColor = \"#000000\",\n    floodOpacity = 0.15\n  } = {}\n) {\n  return `\n<filter id=\"${esc(id)}\" x=\"-25%\" y=\"-25%\" width=\"150%\" height=\"160%\">\n  <feDropShadow\n    dx=\"${dx}\"\n    dy=\"${dy}\"\n    stdDeviation=\"${stdDeviation}\"\n    flood-color=\"${floodColor}\"\n    flood-opacity=\"${floodOpacity}\"/>\n</filter>`;\n}\n\nfunction linearGradient(\n  id,\n  stops,\n  {\n    x1 = \"0%\",\n    y1 = \"0%\",\n    x2 = \"100%\",\n    y2 = \"100%\"\n  } = {}\n) {\n  const body = (Array.isArray(stops) ? stops : [])\n    .map(stop => `<stop ${attrs({\n      offset: stop.offset,\n      \"stop-color\": stop.color,\n      \"stop-opacity\": stop.opacity\n    })}/>`).join(\"\");\n\n  return `<linearGradient ${attrs({ id, x1, y1, x2, y2 })}>${body}</linearGradient>`;\n}\n\nmodule.exports = {\n  SVG_VERSION,\n  SVG_NAMESPACE,\n  esc,\n  attrs,\n  svg,\n  end,\n  defs,\n  group,\n  rect,\n  circle,\n  line,\n  image,\n  text,\n  path,\n  clipPath,\n  shadowFilter,\n  linearGradient\n};\n", "passport/components/logo.js": "/**\n * DiArt Passport\n * File: passport/components/logo.js\n * Version: 2.0.0\n *\n * Renders the official DiArt emblem from an external URL.\n */\n\n\"use strict\";\n\nconst LOGO_VERSION = \"2.0.0\";\n\nfunction esc(value) {\n  return String(value ?? \"\")\n    .replace(/&/g, \"&amp;\")\n    .replace(/</g, \"&lt;\")\n    .replace(/>/g, \"&gt;\")\n    .replace(/\"/g, \"&quot;\")\n    .replace(/'/g, \"&apos;\");\n}\n\nfunction drawLogo({\n  href = \"\",\n  x,\n  y,\n  width,\n  height,\n  opacity = 1,\n  preserveAspectRatio = \"xMidYMid meet\",\n  fallbackText = \"DiArt\",\n  fallbackColor = \"#5B2A0C\"\n}) {\n  if (href) {\n    return `\n<image\n  href=\"${esc(href)}\"\n  x=\"${x}\"\n  y=\"${y}\"\n  width=\"${width}\"\n  height=\"${height}\"\n  opacity=\"${Math.max(0, Math.min(1, Number(opacity) || 1))}\"\n  preserveAspectRatio=\"${esc(preserveAspectRatio)}\"/>`;\n  }\n\n  return `\n<text\n  x=\"${x + width / 2}\"\n  y=\"${y + height / 2 + 18}\"\n  text-anchor=\"middle\"\n  font-family=\"Georgia, 'Times New Roman', serif\"\n  font-size=\"${Math.min(width, height) * 0.28}\"\n  font-weight=\"700\"\n  fill=\"${fallbackColor}\">\n  ${esc(fallbackText)}\n</text>`;\n}\n\nmodule.exports = {\n  LOGO_VERSION,\n  drawLogo\n};\n", "passport/components/photo.js": "/**\n * DiArt Passport\n * File: passport/components/photo.js\n * Version: 2.0.0\n *\n * Renders:\n * - main portrait\n * - circular photo samples for eye, hair and skin\n *\n * Supports:\n * - direct image URLs\n * - normalized crop regions\n * - safe XML escaping\n * - unique clipPath IDs\n */\n\n\"use strict\";\n\nconst PHOTO_COMPONENT_VERSION = \"2.0.0\";\n\nfunction esc(value) {\n  return String(value ?? \"\")\n    .replace(/&/g, \"&amp;\")\n    .replace(/</g, \"&lt;\")\n    .replace(/>/g, \"&gt;\")\n    .replace(/\"/g, \"&quot;\")\n    .replace(/'/g, \"&apos;\");\n}\n\nfunction clamp(value, min, max) {\n  const number = Number(value);\n\n  if (!Number.isFinite(number)) {\n    return min;\n  }\n\n  return Math.min(max, Math.max(min, number));\n}\n\nfunction safeId(value, fallback) {\n  const text = String(value || fallback || \"clip\")\n    .trim()\n    .replace(/[^a-zA-Z0-9_-]+/g, \"_\")\n    .replace(/^_+|_+$/g, \"\");\n\n  return text || fallback || \"clip\";\n}\n\nfunction imageElement({\n  href,\n  x,\n  y,\n  width,\n  height,\n  clipPathId = \"\",\n  preserveAspectRatio = \"xMidYMid slice\",\n  opacity = 1\n}) {\n  if (!href) return \"\";\n\n  return `\n<image\n  href=\"${esc(href)}\"\n  x=\"${x}\"\n  y=\"${y}\"\n  width=\"${width}\"\n  height=\"${height}\"\n  opacity=\"${clamp(opacity, 0, 1)}\"\n  preserveAspectRatio=\"${esc(preserveAspectRatio)}\"\n  ${clipPathId ? `clip-path=\"url(#${safeId(clipPathId)})\"` : \"\"}/>`;\n}\n\nfunction portrait({\n  href,\n  x,\n  y,\n  width,\n  height,\n  radius = 28,\n  clipId = \"portraitClip\",\n  borderColor = \"#FFFFFF\",\n  borderWidth = 0,\n  background = \"#F1E8DF\"\n}) {\n  const id = safeId(clipId, \"portraitClip\");\n\n  let out = `\n<defs>\n  <clipPath id=\"${id}\">\n    <rect\n      x=\"${x}\"\n      y=\"${y}\"\n      width=\"${width}\"\n      height=\"${height}\"\n      rx=\"${radius}\"/>\n  </clipPath>\n</defs>\n\n<rect\n  x=\"${x}\"\n  y=\"${y}\"\n  width=\"${width}\"\n  height=\"${height}\"\n  rx=\"${radius}\"\n  fill=\"${background}\"/>`;\n\n  out += imageElement({\n    href,\n    x,\n    y,\n    width,\n    height,\n    clipPathId: id\n  });\n\n  if (borderWidth > 0) {\n    out += `\n<rect\n  x=\"${x}\"\n  y=\"${y}\"\n  width=\"${width}\"\n  height=\"${height}\"\n  rx=\"${radius}\"\n  fill=\"none\"\n  stroke=\"${borderColor}\"\n  stroke-width=\"${borderWidth}\"/>`;\n  }\n\n  return out;\n}\n\nfunction normalizedCropToImageBox({\n  region,\n  targetX,\n  targetY,\n  targetWidth,\n  targetHeight\n}) {\n  const safeRegion = region && typeof region === \"object\"\n    ? region\n    : {};\n\n  const rx = clamp(safeRegion.x, 0, 1);\n  const ry = clamp(safeRegion.y, 0, 1);\n  const rw = clamp(safeRegion.width, 0.01, 1);\n  const rh = clamp(safeRegion.height, 0.01, 1);\n\n  const scale = Math.max(\n    targetWidth / rw,\n    targetHeight / rh\n  );\n\n  return {\n    x: targetX - rx * scale,\n    y: targetY - ry * scale,\n    width: scale,\n    height: scale\n  };\n}\n\nfunction circularSample({\n  id,\n  href,\n  x,\n  y,\n  size,\n  region = null,\n  fill = \"#CCCCCC\",\n  borderColor = \"#FFFFFF\",\n  borderWidth = 4,\n  shadow = false\n}) {\n  const clipId = safeId(id, \"sampleClip\");\n  const radius = size / 2;\n  const cx = x + radius;\n  const cy = y + radius;\n\n  let out = `\n<defs>\n  <clipPath id=\"${clipId}\">\n    <circle cx=\"${cx}\" cy=\"${cy}\" r=\"${radius}\"/>\n  </clipPath>\n</defs>\n\n<circle\n  cx=\"${cx}\"\n  cy=\"${cy}\"\n  r=\"${radius}\"\n  fill=\"${fill}\"\n  ${shadow ? 'filter=\"url(#shadow)\"' : \"\"}/>`;\n\n  if (href) {\n    if (region && typeof region === \"object\") {\n      const box = normalizedCropToImageBox({\n        region,\n        targetX: x,\n        targetY: y,\n        targetWidth: size,\n        targetHeight: size\n      });\n\n      out += imageElement({\n        href,\n        x: box.x,\n        y: box.y,\n        width: box.width,\n        height: box.height,\n        clipPathId: clipId\n      });\n    } else {\n      out += imageElement({\n        href,\n        x,\n        y,\n        width: size,\n        height: size,\n        clipPathId: clipId\n      });\n    }\n  }\n\n  out += `\n<circle\n  cx=\"${cx}\"\n  cy=\"${cy}\"\n  r=\"${radius}\"\n  fill=\"none\"\n  stroke=\"${borderColor}\"\n  stroke-width=\"${borderWidth}\"/>`;\n\n  return out;\n}\n\nfunction eyeSample(options) {\n  return circularSample({\n    ...options,\n    id: options.id || \"eyeSampleClip\"\n  });\n}\n\nfunction hairSample(options) {\n  return circularSample({\n    ...options,\n    id: options.id || \"hairSampleClip\"\n  });\n}\n\nfunction skinSample(options) {\n  return circularSample({\n    ...options,\n    id: options.id || \"skinSampleClip\"\n  });\n}\n\nmodule.exports = {\n  PHOTO_COMPONENT_VERSION,\n  portrait,\n  circularSample,\n  eyeSample,\n  hairSample,\n  skinSample,\n  normalizedCropToImageBox\n};\n", "passport/page1.js": "/**\n * DiArt Color Passport\n * File: passport/page1.js\n * Version: 3.0.0-approved\n *\n * Approved first page:\n * - official DiArt emblem\n * - season identity\n * - confidence block\n * - portrait\n * - four analysis scales\n * - natural palette\n * - Top-3 donut and nearest seasons\n * - service metadata\n * - branded footer\n */\n\n\"use strict\";\n\nconst layout = require(\"./layout\");\nconst svg = require(\"./svg\");\nconst { drawLogo } = require(\"./components/logo\");\nconst { portrait } = require(\"./components/photo\");\n\nconst PAGE_1_VERSION = \"3.0.0-approved\";\n\nfunction obj(value) {\n  return value && typeof value === \"object\" && !Array.isArray(value)\n    ? value\n    : {};\n}\n\nfunction arr(value) {\n  return Array.isArray(value) ? value : [];\n}\n\nfunction esc(value) {\n  return String(value ?? \"\")\n    .replace(/&/g, \"&amp;\")\n    .replace(/</g, \"&lt;\")\n    .replace(/>/g, \"&gt;\")\n    .replace(/\"/g, \"&quot;\")\n    .replace(/'/g, \"&apos;\");\n}\n\nfunction clamp(value, min = 0, max = 1) {\n  const number = Number(value);\n  if (!Number.isFinite(number)) return min;\n  return Math.min(max, Math.max(min, number));\n}\n\nfunction text({\n  value,\n  x,\n  y,\n  size,\n  weight = 400,\n  family = \"Arial, Helvetica, sans-serif\",\n  fill = \"#24160F\",\n  anchor = \"start\",\n  tracking = 0,\n  style = \"\"\n}) {\n  return `<text x=\"${x}\" y=\"${y}\" text-anchor=\"${anchor}\"\n    font-family=\"${family}\" font-size=\"${size}\" font-weight=\"${weight}\"\n    letter-spacing=\"${tracking}\" fill=\"${fill}\" style=\"${style}\">${esc(value)}</text>`;\n}\n\nfunction panel(box, fill = \"#FFFDFC\", stroke = \"#E7DDD3\", radius = 16) {\n  return `<rect x=\"${box.x}\" y=\"${box.y}\" width=\"${box.width}\" height=\"${box.height}\"\n    rx=\"${radius}\" fill=\"${fill}\" stroke=\"${stroke}\" stroke-width=\"1\"/>`;\n}\n\nfunction wrapWords(value, maxChars) {\n  const words = String(value || \"\").trim().split(/\\s+/).filter(Boolean);\n  const lines = [];\n  let current = \"\";\n\n  for (const word of words) {\n    const candidate = current ? `${current} ${word}` : word;\n    if (!current || candidate.length <= maxChars) {\n      current = candidate;\n    } else {\n      lines.push(current);\n      current = word;\n    }\n  }\n\n  if (current) lines.push(current);\n  return lines;\n}\n\nfunction wrappedText({\n  value,\n  x,\n  y,\n  maxChars,\n  maxLines,\n  lineHeight,\n  size,\n  weight = 400,\n  family = \"Arial, Helvetica, sans-serif\",\n  fill = \"#24160F\",\n  anchor = \"start\"\n}) {\n  return wrapWords(value, maxChars)\n    .slice(0, maxLines)\n    .map((line, index) =>\n      text({\n        value: line,\n        x,\n        y: y + index * lineHeight,\n        size,\n        weight,\n        family,\n        fill,\n        anchor\n      })\n    )\n    .join(\"\");\n}\n\nfunction getPin(manifest, seasonId) {\n  const normalized = seasonId === \"true_spring\" ? \"warm_spring\" : seasonId;\n  return arr(obj(manifest).pins).find(pin => pin && pin.slug === normalized) || null;\n}\n\nfunction themeFrom(data) {\n  const seasonId = obj(data.season).id;\n  const manifest = obj(obj(data.assets).pin_manifest);\n  const pin = getPin(manifest, seasonId);\n\n  return {\n    accent: pin && pin.color ? pin.color : \"#7B3F20\",\n    ink: \"#24160F\",\n    muted: \"#6C5B50\",\n    line: \"#E7DDD3\",\n    soft: \"#F8F0E8\",\n    panel: \"#FFFDFC\",\n    white: \"#FFFFFF\"\n  };\n}\n\nfunction pinShape({\n  cx,\n  cy,\n  size,\n  color,\n  symbol = \"✦\",\n  selected = false\n}) {\n  const top = cy - size * 0.46;\n  const bottom = cy + size * 0.54;\n  const left = cx - size * 0.34;\n  const right = cx + size * 0.34;\n  const r = size * 0.31;\n\n  return `\n<g>\n  <path d=\"\n    M ${cx} ${bottom}\n    C ${cx - size * 0.10} ${cy + size * 0.36},\n      ${left} ${cy + size * 0.06},\n      ${left} ${cy - size * 0.10}\n    A ${r} ${r} 0 1 1 ${right} ${cy - size * 0.10}\n    C ${right} ${cy + size * 0.06},\n      ${cx + size * 0.10} ${cy + size * 0.36},\n      ${cx} ${bottom}\n    Z\"\n    fill=\"${color}\"\n    ${selected ? `stroke=\"#FFFFFF\" stroke-width=\"${Math.max(1.4, size * 0.04)}\"` : \"\"}/>\n  <text x=\"${cx}\" y=\"${top + size * 0.48}\" text-anchor=\"middle\"\n    font-family=\"Georgia, 'Times New Roman', serif\"\n    font-size=\"${size * 0.31}\" font-weight=\"700\" fill=\"#FFFFFF\">${esc(symbol)}</text>\n</g>`;\n}\n\nfunction seasonSymbol(seasonId) {\n  const map = {\n    light_spring: \"✿\",\n    true_spring: \"☼\",\n    warm_spring: \"☼\",\n    bright_spring: \"✹\",\n    light_summer: \"✿\",\n    true_summer: \"✦\",\n    soft_summer: \"✿\",\n    soft_autumn: \"❧\",\n    true_autumn: \"❉\",\n    deep_autumn: \"✦\",\n    bright_winter: \"✦\",\n    true_winter: \"❄\",\n    deep_winter: \"◆\"\n  };\n  return map[seasonId] || \"✦\";\n}\n\nfunction renderIdentity(data, theme) {\n  const L = layout.PAGE_1;\n  const season = obj(data.season);\n  const confidence = obj(data.confidence);\n  const assets = obj(data.assets);\n\n  let out = \"\";\n\n  out += drawLogo({\n    href: assets.logo,\n    x: L.logo.x,\n    y: L.logo.y,\n    width: L.logo.width,\n    height: L.logo.height\n  });\n\n  out += text({\n    value: \"ТВОЙ ЦВЕТОТИП\",\n    x: L.identity.x + L.identity.width / 2,\n    y: L.identity.eyebrowY,\n    size: 14,\n    weight: 700,\n    family: \"Georgia, 'Times New Roman', serif\",\n    fill: theme.ink,\n    anchor: \"middle\",\n    tracking: 1.1\n  });\n\n  out += text({\n    value: season.name_ru || \"\",\n    x: L.identity.x + L.identity.width / 2,\n    y: L.identity.titleRuY,\n    size: 38,\n    weight: 700,\n    family: \"Georgia, 'Times New Roman', serif\",\n    fill: theme.ink,\n    anchor: \"middle\"\n  });\n\n  out += text({\n    value: season.name_en || \"\",\n    x: L.identity.x + L.identity.width / 2,\n    y: L.identity.titleEnY,\n    size: 22,\n    weight: 500,\n    family: \"Georgia, 'Times New Roman', serif\",\n    fill: theme.ink,\n    anchor: \"middle\"\n  });\n\n  out += wrappedText({\n    value: season.description_ru || \"\",\n    x: L.identity.x + L.identity.width / 2,\n    y: L.identity.descriptionY,\n    maxChars: 34,\n    maxLines: 2,\n    lineHeight: L.identity.descriptionLineHeight,\n    size: 15,\n    family: \"Georgia, 'Times New Roman', serif\",\n    fill: theme.ink,\n    anchor: \"middle\"\n  });\n\n  out += panel(L.confidence, \"#FFFDFC\", theme.line, 14);\n\n  out += text({\n    value: \"ДОСТОВЕРНОСТЬ АНАЛИЗА\",\n    x: L.confidence.x + L.confidence.width / 2,\n    y: L.confidence.labelY,\n    size: 8.5,\n    weight: 700,\n    fill: theme.ink,\n    anchor: \"middle\"\n  });\n\n  out += text({\n    value: `${confidence.percent ?? 0}%`,\n    x: L.confidence.x + L.confidence.width / 2,\n    y: L.confidence.percentY,\n    size: 50,\n    weight: 700,\n    family: \"Georgia, 'Times New Roman', serif\",\n    fill: theme.ink,\n    anchor: \"middle\"\n  });\n\n  out += text({\n    value: \"★ ★ ★ ★ ★\",\n    x: L.confidence.x + L.confidence.width / 2,\n    y: L.confidence.starsY,\n    size: 18,\n    weight: 700,\n    fill: theme.accent,\n    anchor: \"middle\",\n    tracking: 1\n  });\n\n  out += `<rect x=\"${L.confidence.pillX}\" y=\"${L.confidence.pillY}\"\n    width=\"${L.confidence.pillWidth}\" height=\"${L.confidence.pillHeight}\"\n    rx=\"${L.confidence.pillHeight / 2}\" fill=\"${theme.soft}\"/>`;\n\n  out += text({\n    value: confidence.label_ru || \"\",\n    x: L.confidence.pillX + L.confidence.pillWidth / 2,\n    y: L.confidence.pillY + 16,\n    size: 8.5,\n    weight: 700,\n    fill: theme.ink,\n    anchor: \"middle\"\n  });\n\n  return out;\n}\n\nfunction scaleIcon(type, cx, cy, theme) {\n  const r = 20;\n  let icon = \"\";\n\n  if (type === \"temperature\") {\n    icon = `\n      <line x1=\"${cx}\" y1=\"${cy - 9}\" x2=\"${cx}\" y2=\"${cy + 5}\"\n        stroke=\"${theme.accent}\" stroke-width=\"3\" stroke-linecap=\"round\"/>\n      <circle cx=\"${cx}\" cy=\"${cy + 9}\" r=\"5\" fill=\"${theme.accent}\"/>\n      <circle cx=\"${cx}\" cy=\"${cy - 10}\" r=\"3\" fill=\"none\"\n        stroke=\"${theme.accent}\" stroke-width=\"2\"/>`;\n  } else if (type === \"depth\") {\n    icon = `\n      <path d=\"M ${cx - 11} ${cy - 6} Q ${cx - 5} ${cy - 12} ${cx} ${cy - 6}\n               Q ${cx + 5} ${cy} ${cx + 11} ${cy - 6}\"\n        fill=\"none\" stroke=\"${theme.accent}\" stroke-width=\"2.5\"/>\n      <path d=\"M ${cx - 11} ${cy + 2} Q ${cx - 5} ${cy - 4} ${cx} ${cy + 2}\n               Q ${cx + 5} ${cy + 8} ${cx + 11} ${cy + 2}\"\n        fill=\"none\" stroke=\"${theme.accent}\" stroke-width=\"2.5\"/>\n      <path d=\"M ${cx - 11} ${cy + 10} Q ${cx - 5} ${cy + 4} ${cx} ${cy + 10}\n               Q ${cx + 5} ${cy + 16} ${cx + 11} ${cy + 10}\"\n        fill=\"none\" stroke=\"${theme.accent}\" stroke-width=\"2.5\"/>`;\n  } else if (type === \"contrast\") {\n    icon = `\n      <circle cx=\"${cx}\" cy=\"${cy}\" r=\"11\" fill=\"#FFFFFF\"\n        stroke=\"${theme.accent}\" stroke-width=\"2\"/>\n      <path d=\"M ${cx} ${cy - 11}\n               A 11 11 0 0 0 ${cx} ${cy + 11} Z\"\n        fill=\"${theme.accent}\"/>`;\n  } else {\n    icon = `\n      <text x=\"${cx}\" y=\"${cy + 7}\" text-anchor=\"middle\"\n        font-family=\"Georgia, 'Times New Roman', serif\"\n        font-size=\"24\" fill=\"${theme.accent}\">✦</text>`;\n  }\n\n  return `\n<g>\n  <circle cx=\"${cx}\" cy=\"${cy}\" r=\"${r}\" fill=\"${theme.soft}\"\n    stroke=\"${theme.line}\" stroke-width=\"1\"/>\n  ${icon}\n</g>`;\n}\n\nfunction renderScaleRow({\n  type,\n  titleRu,\n  titleEn,\n  leftRu,\n  leftEn,\n  rightRu,\n  rightEn,\n  position,\n  top,\n  theme\n}) {\n  const L = layout.PAGE_1.scalesPanel;\n  const cy = top + 39;\n  const markerX = L.scaleX1 + clamp(position) * (L.scaleX2 - L.scaleX1);\n\n  let out = \"\";\n\n  out += scaleIcon(type, L.iconX, cy, theme);\n\n  out += text({\n    value: titleRu,\n    x: L.titleX,\n    y: top + 25,\n    size: 14,\n    weight: 700,\n    family: \"Georgia, 'Times New Roman', serif\",\n    fill: theme.ink\n  });\n\n  out += text({\n    value: titleEn,\n    x: L.titleX,\n    y: top + 43,\n    size: 10,\n    fill: theme.ink\n  });\n\n  out += text({\n    value: leftRu,\n    x: L.labelsX,\n    y: top + 25,\n    size: 9.5,\n    fill: theme.ink\n  });\n\n  out += text({\n    value: leftEn,\n    x: L.labelsX,\n    y: top + 42,\n    size: 8.5,\n    fill: theme.muted\n  });\n\n  out += text({\n    value: rightRu,\n    x: L.scaleX2 + 18,\n    y: top + 25,\n    size: 9.5,\n    fill: theme.ink,\n    anchor: \"end\"\n  });\n\n  out += text({\n    value: rightEn,\n    x: L.scaleX2 + 18,\n    y: top + 42,\n    size: 8.5,\n    fill: theme.muted,\n    anchor: \"end\"\n  });\n\n  out += `<line x1=\"${L.scaleX1}\" y1=\"${top + 53}\"\n    x2=\"${L.scaleX2}\" y2=\"${top + 53}\"\n    stroke=\"${theme.line}\" stroke-width=\"4\" stroke-linecap=\"round\"/>`;\n\n  out += `<line x1=\"${L.scaleX1}\" y1=\"${top + 53}\"\n    x2=\"${markerX}\" y2=\"${top + 53}\"\n    stroke=\"${theme.accent}\" stroke-width=\"4\" stroke-linecap=\"round\"/>`;\n\n  out += pinShape({\n    cx: markerX,\n    cy: top + 50,\n    size: 28,\n    color: theme.ink,\n    symbol: \"✦\",\n    selected: false\n  });\n\n  out += `<line x1=\"${L.x + 24}\" y1=\"${top + L.rowHeight - 1}\"\n    x2=\"${L.x + L.width - 24}\" y2=\"${top + L.rowHeight - 1}\"\n    stroke=\"${theme.line}\" stroke-width=\"0.8\"/>`;\n\n  return out;\n}\n\nfunction renderPortraitAndScales(data, theme) {\n  const L = layout.PAGE_1;\n  const scales = obj(data.scales);\n  let out = \"\";\n\n  out += panel(L.portrait, \"#FFFFFF\", theme.line, L.portrait.radius);\n\n  out += portrait({\n    href: obj(data.source).photo_url,\n    x: L.portrait.x + 3,\n    y: L.portrait.y + 3,\n    width: L.portrait.width - 6,\n    height: L.portrait.height - 6,\n    radius: L.portrait.radius - 3,\n    borderWidth: 0\n  });\n\n  out += panel(L.scalesPanel, theme.panel, theme.line, L.scalesPanel.radius);\n\n  out += renderScaleRow({\n    type: \"temperature\",\n    titleRu: \"ТЕМПЕРАТУРА\",\n    titleEn: \"Temperature\",\n    leftRu: \"Холодная\",\n    leftEn: \"Cool\",\n    rightRu: \"Тёплая\",\n    rightEn: \"Warm\",\n    position: obj(scales.temperature).position,\n    top: L.scalesPanel.rows.temperature.top,\n    theme\n  });\n\n  out += renderScaleRow({\n    type: \"depth\",\n    titleRu: \"ГЛУБИНА\",\n    titleEn: \"Depth\",\n    leftRu: \"Светлая\",\n    leftEn: \"Light\",\n    rightRu: \"Глубокая\",\n    rightEn: \"Deep\",\n    position: obj(scales.depth).position,\n    top: L.scalesPanel.rows.depth.top,\n    theme\n  });\n\n  out += renderScaleRow({\n    type: \"contrast\",\n    titleRu: \"КОНТРАСТ\",\n    titleEn: \"Contrast\",\n    leftRu: \"Низкий\",\n    leftEn: \"Low\",\n    rightRu: \"Высокий\",\n    rightEn: \"High\",\n    position: obj(scales.contrast).position,\n    top: L.scalesPanel.rows.contrast.top,\n    theme\n  });\n\n  out += renderScaleRow({\n    type: \"clarity\",\n    titleRu: \"ЧИСТОТА\",\n    titleEn: \"Clarity\",\n    leftRu: \"Мягкая\",\n    leftEn: \"Soft\",\n    rightRu: \"Чистая\",\n    rightEn: \"Clear\",\n    position: obj(scales.clarity).position,\n    top: L.scalesPanel.rows.clarity.top,\n    theme\n  });\n\n  return out;\n}\n\nfunction sampleCircle({\n  id,\n  href,\n  fill,\n  cx,\n  cy,\n  size,\n  theme\n}) {\n  const r = size / 2;\n\n  let out = `\n<defs>\n  <clipPath id=\"${id}\">\n    <circle cx=\"${cx}\" cy=\"${cy}\" r=\"${r}\"/>\n  </clipPath>\n</defs>`;\n\n  if (href) {\n    out += `<image href=\"${esc(href)}\"\n      x=\"${cx - r}\" y=\"${cy - r}\" width=\"${size}\" height=\"${size}\"\n      clip-path=\"url(#${id})\" preserveAspectRatio=\"xMidYMid slice\"/>`;\n  } else {\n    out += `<circle cx=\"${cx}\" cy=\"${cy}\" r=\"${r}\" fill=\"${fill}\"/>`;\n  }\n\n  out += `<circle cx=\"${cx}\" cy=\"${cy}\" r=\"${r}\"\n    fill=\"none\" stroke=\"${theme.accent}\" stroke-width=\"1.5\"/>`;\n\n  return out;\n}\n\nfunction renderNaturalItem({\n  key,\n  label,\n  column,\n  sample,\n  natural,\n  theme\n}) {\n  const L = layout.PAGE_1.naturalPalette;\n  const cx = column.centerX;\n  const cropCy = L.cropY + L.cropSize / 2;\n  const source = obj(sample);\n  const fallback = obj(natural);\n  const fill = source.hex || fallback.hex || \"#CCCCCC\";\n  const name = source.name_ru || fallback.name_ru || label;\n  const swatches = arr(source.swatches).length\n    ? arr(source.swatches).slice(0, 5)\n    : [fill, fill, fill, fill, fill];\n\n  let out = \"\";\n\n  out += text({\n    value: label,\n    x: cx,\n    y: L.itemY,\n    size: 10,\n    weight: 700,\n    family: \"Georgia, 'Times New Roman', serif\",\n    fill: theme.ink,\n    anchor: \"middle\"\n  });\n\n  out += sampleCircle({\n    id: `${key}ApprovedClip`,\n    href: source.url,\n    fill,\n    cx,\n    cy: cropCy,\n    size: L.cropSize,\n    theme\n  });\n\n  const totalWidth =\n    swatches.length * L.swatchSize +\n    (swatches.length - 1) * L.swatchGap;\n  const startX = cx - totalWidth / 2;\n\n  swatches.forEach((color, index) => {\n    out += `<circle cx=\"${startX + index * (L.swatchSize + L.swatchGap) + L.swatchSize / 2}\"\n      cy=\"${L.swatchY + L.swatchSize / 2}\" r=\"${L.swatchSize / 2}\"\n      fill=\"${color}\"/>`;\n  });\n\n  out += wrappedText({\n    value: name,\n    x: cx,\n    y: L.nameY,\n    maxChars: 15,\n    maxLines: 2,\n    lineHeight: 17,\n    size: 10.5,\n    family: \"Georgia, 'Times New Roman', serif\",\n    fill: theme.ink,\n    anchor: \"middle\"\n  });\n\n  out += text({\n    value: `HEX ${fill}`,\n    x: cx,\n    y: L.hexY,\n    size: 9.5,\n    family: \"Georgia, 'Times New Roman', serif\",\n    fill: theme.ink,\n    anchor: \"middle\"\n  });\n\n  return out;\n}\n\nfunction renderNaturalPalette(data, theme) {\n  const L = layout.PAGE_1.naturalPalette;\n  const samples = obj(data.photo_samples);\n  const natural = obj(data.natural_colors);\n\n  let out = panel(L, theme.panel, theme.line, L.radius);\n\n  out += text({\n    value: \"ТВОЯ ПРИРОДНАЯ ПАЛИТРА\",\n    x: L.x + L.width / 2,\n    y: L.titleY,\n    size: 14,\n    weight: 700,\n    family: \"Georgia, 'Times New Roman', serif\",\n    fill: theme.ink,\n    anchor: \"middle\"\n  });\n\n  out += renderNaturalItem({\n    key: \"eye\",\n    label: \"ГЛАЗА\",\n    column: L.columns[0],\n    sample: samples.eye,\n    natural: natural.eye,\n    theme\n  });\n\n  out += renderNaturalItem({\n    key: \"hair\",\n    label: \"ВОЛОСЫ\",\n    column: L.columns[1],\n    sample: samples.hair,\n    natural: natural.hair,\n    theme\n  });\n\n  out += renderNaturalItem({\n    key: \"skin\",\n    label: \"КОЖА\",\n    column: L.columns[2],\n    sample: samples.skin,\n    natural: natural.skin,\n    theme\n  });\n\n  return out;\n}\n\nfunction polar(cx, cy, radius, angleDegrees) {\n  const angle = (angleDegrees - 90) * Math.PI / 180;\n  return {\n    x: cx + Math.cos(angle) * radius,\n    y: cy + Math.sin(angle) * radius\n  };\n}\n\nfunction donutSegment({\n  cx,\n  cy,\n  outerRadius,\n  innerRadius,\n  startAngle,\n  endAngle,\n  fill\n}) {\n  const startOuter = polar(cx, cy, outerRadius, endAngle);\n  const endOuter = polar(cx, cy, outerRadius, startAngle);\n  const startInner = polar(cx, cy, innerRadius, startAngle);\n  const endInner = polar(cx, cy, innerRadius, endAngle);\n  const largeArc = endAngle - startAngle > 180 ? 1 : 0;\n\n  return `<path d=\"\n    M ${startOuter.x} ${startOuter.y}\n    A ${outerRadius} ${outerRadius} 0 ${largeArc} 0 ${endOuter.x} ${endOuter.y}\n    L ${startInner.x} ${startInner.y}\n    A ${innerRadius} ${innerRadius} 0 ${largeArc} 1 ${endInner.x} ${endInner.y}\n    Z\"\n    fill=\"${fill}\" stroke=\"#FFFFFF\" stroke-width=\"1.2\"/>`;\n}\n\nfunction renderNearestSeasons(data, theme) {\n  const L = layout.PAGE_1.nearestSeasons;\n  const top3 = arr(data.top3).slice(0, 3);\n  const manifest = obj(obj(data.assets).pin_manifest);\n  const total = top3.reduce(\n    (sum, item) => sum + Math.max(0, Number(item.score) || 0),\n    0\n  ) || 1;\n\n  let out = panel(L, theme.panel, theme.line, L.radius);\n\n  out += text({\n    value: \"БЛИЖАЙШИЕ ЦВЕТОТИПЫ\",\n    x: L.x + L.width / 2,\n    y: L.titleY,\n    size: 14,\n    weight: 700,\n    family: \"Georgia, 'Times New Roman', serif\",\n    fill: theme.ink,\n    anchor: \"middle\"\n  });\n\n  let angle = 0;\n\n  top3.forEach((item, index) => {\n    const value = Math.max(0, Number(item.score) || 0);\n    const sweep = value / total * 360;\n    const pin = getPin(manifest, item.season_id);\n    const color = pin && pin.color\n      ? pin.color\n      : [theme.accent, \"#7B7A3D\", \"#23364A\"][index];\n\n    out += donutSegment({\n      cx: L.donutCenterX,\n      cy: L.donutCenterY,\n      outerRadius: L.donutOuterRadius,\n      innerRadius: L.donutInnerRadius,\n      startAngle: angle,\n      endAngle: angle + sweep,\n      fill: color\n    });\n\n    const mid = angle + sweep / 2;\n    const labelPoint = polar(\n      L.donutCenterX,\n      L.donutCenterY,\n      (L.donutOuterRadius + L.donutInnerRadius) / 2,\n      mid\n    );\n\n    out += text({\n      value: `${Math.round(value)}%`,\n      x: labelPoint.x,\n      y: labelPoint.y + 4,\n      size: 11,\n      weight: 700,\n      fill: theme.white,\n      anchor: \"middle\"\n    });\n\n    angle += sweep;\n  });\n\n  out += `<circle cx=\"${L.donutCenterX}\" cy=\"${L.donutCenterY}\"\n    r=\"${L.donutInnerRadius - 2}\" fill=\"#FFFDFC\"/>`;\n\n  out += text({\n    value: \"DiArt\",\n    x: L.donutCenterX,\n    y: L.donutCenterY + 5,\n    size: 19,\n    weight: 700,\n    family: \"Georgia, 'Times New Roman', serif\",\n    fill: theme.ink,\n    anchor: \"middle\",\n    style: \"font-style:italic\"\n  });\n\n  top3.forEach((item, index) => {\n    const rowY = L.listY + index * L.listRowHeight;\n    const pin = getPin(manifest, item.season_id);\n    const color = pin && pin.color\n      ? pin.color\n      : [theme.accent, \"#7B7A3D\", \"#23364A\"][index];\n\n    out += pinShape({\n      cx: L.listX + 18,\n      cy: rowY + 18,\n      size: L.pinSize,\n      color,\n      symbol: seasonSymbol(item.season_id),\n      selected: index === 0\n    });\n\n    out += text({\n      value: item.name_ru || \"\",\n      x: L.listX + 45,\n      y: rowY + 15,\n      size: 10.5,\n      weight: 600,\n      family: \"Georgia, 'Times New Roman', serif\",\n      fill: theme.ink\n    });\n\n    out += text({\n      value: `${Math.round(Number(item.score) || 0)}%`,\n      x: L.x + L.width - 20,\n      y: rowY + 19,\n      size: 13,\n      weight: 700,\n      family: \"Georgia, 'Times New Roman', serif\",\n      fill: theme.ink,\n      anchor: \"end\"\n    });\n  });\n\n  out += wrappedText({\n    value: \"Эти цветотипы имеют наиболее близкие характеристики к вашему природному колориту.\",\n    x: L.x + L.width / 2,\n    y: L.noteY,\n    maxChars: 52,\n    maxLines: 2,\n    lineHeight: 16,\n    size: 10.5,\n    family: \"Georgia, 'Times New Roman', serif\",\n    fill: theme.ink,\n    anchor: \"middle\"\n  });\n\n  return out;\n}\n\nfunction renderMeta(data, theme) {\n  const L = layout.PAGE_1.meta;\n  const passport = obj(data.passport);\n  const items = [\n    [\"Паспорт создан:\", passport.created_at || \"\"],\n    [\"AI-модель:\", passport.ai_model || \"\"],\n    [\"База палитр:\", passport.database_version ? `DiArt DB v${passport.database_version}` : \"\"]\n  ];\n\n  let out = panel(L, theme.panel, theme.line, L.radius);\n\n  items.forEach((item, index) => {\n    const col = L.columns[index];\n    const centerX = col.x + col.width / 2;\n\n    out += text({\n      value: index === 0 ? \"▣\" : index === 1 ? \"✣\" : \"◫\",\n      x: col.x + 14,\n      y: L.iconY + 12,\n      size: 25,\n      fill: theme.accent,\n      anchor: \"middle\"\n    });\n\n    out += text({\n      value: item[0],\n      x: col.x + 35,\n      y: L.labelY,\n      size: 9.5,\n      family: \"Georgia, 'Times New Roman', serif\",\n      fill: theme.ink\n    });\n\n    out += text({\n      value: item[1],\n      x: col.x + 35,\n      y: L.valueY,\n      size: 13,\n      weight: 600,\n      family: \"Georgia, 'Times New Roman', serif\",\n      fill: theme.ink\n    });\n\n    if (index < 2) {\n      const dividerX = index === 0 ? L.divider1X : L.divider2X;\n      out += `<line x1=\"${dividerX}\" y1=\"${L.y + 16}\" x2=\"${dividerX}\"\n        y2=\"${L.y + L.height - 16}\" stroke=\"${theme.line}\" stroke-width=\"1\"/>`;\n    }\n  });\n\n  return out;\n}\n\nfunction renderFooter(data, theme) {\n  const L = layout.PAGE_1.footer;\n  const assets = obj(data.assets);\n\n  let out = \"\";\n\n  out += `<line x1=\"${L.x}\" y1=\"${L.y}\" x2=\"${L.x + L.width}\"\n    y2=\"${L.y}\" stroke=\"${theme.line}\" stroke-width=\"1\"/>`;\n\n  out += drawLogo({\n    href: assets.logo,\n    x: L.logoX,\n    y: L.logoY,\n    width: L.logoWidth,\n    height: L.logoHeight\n  });\n\n  out += text({\n    value: \"✦\",\n    x: L.leftStarX,\n    y: L.sloganY - 2,\n    size: 14,\n    fill: theme.accent,\n    anchor: \"middle\"\n  });\n\n  out += text({\n    value: \"✦\",\n    x: L.rightStarX,\n    y: L.sloganY - 2,\n    size: 14,\n    fill: theme.accent,\n    anchor: \"middle\"\n  });\n\n  out += text({\n    value: \"Ц В Е Т   У К Р А Ш А Е Т   Т Е Б Я\",\n    x: L.x + L.width / 2,\n    y: L.sloganY,\n    size: 9,\n    weight: 600,\n    family: \"Georgia, 'Times New Roman', serif\",\n    fill: theme.ink,\n    anchor: \"middle\",\n    tracking: 2\n  });\n\n  return out;\n}\n\nfunction validate(data) {\n  if (!data || typeof data !== \"object\") {\n    throw new Error(\"Page 1: input data is missing.\");\n  }\n\n  if (!obj(data.season).id) {\n    throw new Error(\"Page 1: season.id is missing.\");\n  }\n\n  if (!obj(data.confidence)) {\n    throw new Error(\"Page 1: confidence is missing.\");\n  }\n\n  if (!obj(data.scales)) {\n    throw new Error(\"Page 1: scales are missing.\");\n  }\n\n  if (!Array.isArray(data.top3) || data.top3.length < 3) {\n    throw new Error(\"Page 1: top3 must contain three seasons.\");\n  }\n\n  if (!obj(obj(data.assets).pin_manifest).pins) {\n    throw new Error(\"Page 1: assets.pin_manifest is missing.\");\n  }\n}\n\nfunction buildPage1(data) {\n  validate(data);\n\n  const theme = themeFrom(data);\n  const P = layout.PAGE;\n\n  let out = svg.svg(P.width, P.height, P.viewBox);\n\n  out += svg.defs(`\n${svg.shadowFilter(\"shadow\", {\n  dx: 0,\n  dy: 3,\n  stdDeviation: 4,\n  floodOpacity: 0.08\n})}\n<linearGradient id=\"approvedPageBackground\" x1=\"0\" y1=\"0\" x2=\"1\" y2=\"1\">\n  <stop offset=\"0%\" stop-color=\"#FFFDFC\"/>\n  <stop offset=\"100%\" stop-color=\"#F8F1E9\"/>\n</linearGradient>`);\n\n  out += `<rect x=\"0\" y=\"0\" width=\"${P.width}\" height=\"${P.height}\"\n    rx=\"${P.outerRadius}\" fill=\"url(#approvedPageBackground)\"/>`;\n\n  out += `<rect x=\"3\" y=\"3\" width=\"${P.width - 6}\" height=\"${P.height - 6}\"\n    rx=\"${P.outerRadius}\" fill=\"none\" stroke=\"${theme.line}\" stroke-width=\"1\"/>`;\n\n  out += renderIdentity(data, theme);\n  out += renderPortraitAndScales(data, theme);\n  out += renderNaturalPalette(data, theme);\n  out += renderNearestSeasons(data, theme);\n  out += renderMeta(data, theme);\n  out += renderFooter(data, theme);\n\n  out += svg.end();\n\n  return out;\n}\n\nmodule.exports = {\n  PAGE_1_VERSION,\n  buildPage1\n};\n", "passport/page2.js": "/**\n * DiArt Color Passport\n * File: passport/page2.js\n * Version: 3.0.0-approved\n */\n\n\"use strict\";\n\nconst layout = require(\"./layout\");\nconst svg = require(\"./svg\");\nconst { drawLogo } = require(\"./components/logo\");\n\nconst PAGE_2_VERSION = \"3.0.0-approved\";\n\nfunction obj(v){return v&&typeof v===\"object\"&&!Array.isArray(v)?v:{};}\nfunction arr(v){return Array.isArray(v)?v:[];}\nfunction esc(v){return String(v??\"\").replace(/&/g,\"&amp;\").replace(/</g,\"&lt;\").replace(/>/g,\"&gt;\").replace(/\"/g,\"&quot;\").replace(/'/g,\"&apos;\");}\n\nfunction text({value,x,y,size,weight=400,family=\"Arial, Helvetica, sans-serif\",fill=\"#24160F\",anchor=\"start\",tracking=0}){\n  return `<text x=\"${x}\" y=\"${y}\" text-anchor=\"${anchor}\" font-family=\"${family}\" font-size=\"${size}\" font-weight=\"${weight}\" letter-spacing=\"${tracking}\" fill=\"${fill}\">${esc(value)}</text>`;\n}\nfunction panel(box,fill=\"#FFFDFC\",stroke=\"#E7DDD3\",radius=16){\n  return `<rect x=\"${box.x}\" y=\"${box.y}\" width=\"${box.width}\" height=\"${box.height}\" rx=\"${radius}\" fill=\"${fill}\" stroke=\"${stroke}\" stroke-width=\"1\"/>`;\n}\nfunction wrapWords(value,maxChars){\n  const words=String(value||\"\").trim().split(/\\s+/).filter(Boolean), lines=[]; let current=\"\";\n  for(const word of words){const c=current?`${current} ${word}`:word;if(!current||c.length<=maxChars)current=c;else{lines.push(current);current=word;}}\n  if(current)lines.push(current); return lines;\n}\nfunction wrapped({value,x,y,maxChars,maxLines,lineHeight,size,weight=400,family=\"Georgia, 'Times New Roman', serif\",fill=\"#24160F\",anchor=\"start\"}){\n  return wrapWords(value,maxChars).slice(0,maxLines).map((line,i)=>text({value:line,x,y:y+i*lineHeight,size,weight,family,fill,anchor})).join(\"\");\n}\nfunction colorName(c){return c&&(c.name_ru||(c.name&&c.name.ru)||c.name_en||(c.name&&c.name.en))||\"Оттенок\";}\nfunction colorHex(c){return c&&(c.hex||c.canonical_hex)||\"#CCCCCC\";}\nfunction groupsFrom(data){\n  const p=obj(data.palette), g=obj(p.groups), colors=arr(p.colors);\n  if(Object.keys(g).length)return {signature:arr(g.signature),core:arr(g.core),additional:arr(g.additional),neutral:arr(g.neutral),accent:arr(g.accent)};\n  return {\n    signature:colors.filter(c=>c&&c.group===\"signature\"),\n    core:colors.filter(c=>c&&c.group===\"core\"),\n    additional:colors.filter(c=>c&&c.group===\"additional\"),\n    neutral:colors.filter(c=>c&&c.group===\"neutral\"),\n    accent:colors.filter(c=>c&&c.group===\"accent\")\n  };\n}\nfunction theme(data){\n  const seasonId=obj(data.season).id, pins=arr(obj(obj(data.assets).pin_manifest).pins);\n  const normalized=seasonId===\"true_spring\"?\"warm_spring\":seasonId;\n  const pin=pins.find(x=>x&&x.slug===normalized);\n  return {accent:pin&&pin.color?pin.color:\"#7B3F20\",ink:\"#24160F\",muted:\"#6C5B50\",line:\"#E7DDD3\",soft:\"#F8F0E8\",panel:\"#FFFDFC\"};\n}\nfunction paletteCard(c,x,y,w,h,colorH,t){\n  const name=colorName(c),hex=colorHex(c);\n  let o=`<rect x=\"${x}\" y=\"${y}\" width=\"${w}\" height=\"${h}\" rx=\"8\" fill=\"#FFFFFF\" stroke=\"${t.line}\" stroke-width=\"1\"/>`;\n  o+=`<rect x=\"${x}\" y=\"${y}\" width=\"${w}\" height=\"${colorH}\" rx=\"8\" fill=\"${hex}\"/><rect x=\"${x}\" y=\"${y+colorH-8}\" width=\"${w}\" height=\"8\" fill=\"${hex}\"/>`;\n  o+=wrapped({value:name,x:x+w/2,y:y+colorH+20,maxChars:14,maxLines:2,lineHeight:14,size:9.5,weight:600,anchor:\"middle\"});\n  o+=text({value:`HEX ${hex}`,x:x+w/2,y:y+h-10,size:8.5,family:\"Georgia, 'Times New Roman', serif\",fill:t.ink,anchor:\"middle\"});\n  return o;\n}\nfunction strip(title,colors,box,t){\n  let o=panel(box,t.panel,t.line,box.radius);\n  o+=text({value:title,x:box.x+box.width/2,y:box.titleY,size:14,weight:700,family:\"Georgia, 'Times New Roman', serif\",anchor:\"middle\"});\n  arr(colors).slice(0,6).forEach((c,i)=>{\n    const x=box.swatchX+i*(box.swatchWidth+box.gap), hex=colorHex(c);\n    o+=`<rect x=\"${x}\" y=\"${box.swatchY}\" width=\"${box.swatchWidth}\" height=\"${box.swatchHeight}\" rx=\"7\" fill=\"${hex}\"/>`;\n    o+=wrapped({value:colorName(c),x:x+box.swatchWidth/2,y:box.nameY,maxChars:10,maxLines:2,lineHeight:12,size:7.5,weight:600,anchor:\"middle\"});\n    o+=text({value:hex,x:x+box.swatchWidth/2,y:box.hexY,size:7.5,family:\"Georgia, 'Times New Roman', serif\",anchor:\"middle\"});\n  });\n  return o;\n}\nfunction usageIcon(index,cx,cy,t){\n  if(index===0)return `<circle cx=\"${cx}\" cy=\"${cy}\" r=\"12\" fill=\"none\" stroke=\"${t.accent}\" stroke-width=\"1.5\"/><circle cx=\"${cx-4}\" cy=\"${cy-2}\" r=\"2\" fill=\"${t.accent}\"/><circle cx=\"${cx+4}\" cy=\"${cy-2}\" r=\"2\" fill=\"${t.accent}\"/><path d=\"M ${cx-6} ${cy+4} Q ${cx} ${cy+9} ${cx+6} ${cy+4}\" fill=\"none\" stroke=\"${t.accent}\" stroke-width=\"1.5\"/>`;\n  if(index===1)return `<path d=\"M ${cx-12} ${cy+8} L ${cx-12} ${cy-4} Q ${cx} ${cy-15} ${cx+12} ${cy-4} L ${cx+12} ${cy+8} Z\" fill=\"none\" stroke=\"${t.accent}\" stroke-width=\"1.5\"/><path d=\"M ${cx} ${cy-14} L ${cx} ${cy-8}\" stroke=\"${t.accent}\" stroke-width=\"1.5\"/>`;\n  if(index===2)return `<path d=\"M ${cx-11} ${cy-9} L ${cx-5} ${cy-13} L ${cx} ${cy-7} L ${cx+5} ${cy-13} L ${cx+11} ${cy-9} L ${cx+8} ${cy+12} L ${cx-8} ${cy+12} Z\" fill=\"none\" stroke=\"${t.accent}\" stroke-width=\"1.5\"/>`;\n  return `<path d=\"M ${cx-10} ${cy-6} L ${cx+10} ${cy-6} L ${cx+8} ${cy+12} L ${cx-8} ${cy+12} Z\" fill=\"none\" stroke=\"${t.accent}\" stroke-width=\"1.5\"/><path d=\"M ${cx-6} ${cy-6} Q ${cx-6} ${cy-14} ${cx} ${cy-14} Q ${cx+6} ${cy-14} ${cx+6} ${cy-6}\" fill=\"none\" stroke=\"${t.accent}\" stroke-width=\"1.5\"/>`;\n}\nfunction buildPage2(data){\n  const P=layout.PAGE,L=layout.PAGE_2,t=theme(data),g=groupsFrom(data);\n  const main=[...g.signature,...g.core,...g.additional].slice(0,12);\n  let o=svg.svg(P.width,P.height,P.viewBox);\n  o+=svg.defs(`${svg.shadowFilter(\"shadow\",{dx:0,dy:3,stdDeviation:4,floodOpacity:.07})}<linearGradient id=\"p2bg\" x1=\"0\" y1=\"0\" x2=\"1\" y2=\"1\"><stop offset=\"0%\" stop-color=\"#FFFDFC\"/><stop offset=\"100%\" stop-color=\"#F8F1E9\"/></linearGradient>`);\n  o+=`<rect x=\"0\" y=\"0\" width=\"${P.width}\" height=\"${P.height}\" rx=\"${P.outerRadius}\" fill=\"url(#p2bg)\"/><rect x=\"3\" y=\"3\" width=\"${P.width-6}\" height=\"${P.height-6}\" rx=\"${P.outerRadius}\" fill=\"none\" stroke=\"${t.line}\"/>`;\n  o+=text({value:\"✦\",x:L.header.leftStarX,y:L.header.titleY,size:17,fill:t.accent,anchor:\"middle\"});\n  o+=text({value:\"ТВОЯ ПАЛИТРА И РЕКОМЕНДАЦИИ\",x:L.header.titleX,y:L.header.titleY,size:18,weight:700,family:\"Georgia, 'Times New Roman', serif\",anchor:\"middle\",tracking:.5});\n  o+=text({value:\"✦\",x:L.header.rightStarX,y:L.header.titleY,size:17,fill:t.accent,anchor:\"middle\"});\n\n  o+=panel(L.palette12,t.panel,t.line,L.palette12.radius);\n  o+=text({value:\"12 ЦВЕТОВ ТВОЕЙ ПАЛИТРЫ\",x:L.palette12.x+L.palette12.width/2,y:L.palette12.titleY,size:12.5,weight:700,family:\"Georgia, 'Times New Roman', serif\",anchor:\"middle\",tracking:.7});\n  main.forEach((c,i)=>{\n    const col=i%6,row=Math.floor(i/6);\n    o+=paletteCard(c,L.palette12.gridX+col*(L.palette12.cardWidth+L.palette12.gapX),L.palette12.gridY+row*(L.palette12.cardHeight+L.palette12.gapY),L.palette12.cardWidth,L.palette12.cardHeight,L.palette12.colorHeight,t);\n  });\n\n  o+=strip(\"НЕЙТРАЛЬНЫЕ ЦВЕТА\",g.neutral,L.neutral,t);\n  o+=strip(\"АКЦЕНТНЫЕ ЦВЕТА\",g.accent,L.accent,t);\n\n  o+=panel(L.usage,t.panel,t.line,L.usage.radius);\n  o+=text({value:\"КАК ИСПОЛЬЗОВАТЬ ПАЛИТРУ\",x:L.usage.titleX,y:L.usage.titleY,size:13.5,weight:700,family:\"Georgia, 'Times New Roman', serif\"});\n  const rules=arr(data.harmony_guide).length>=4?arr(data.harmony_guide).slice(0,4):[\n    \"Носи подходящие оттенки ближе к лицу — они подчеркнут естественную красоту.\",\n    \"Сочетай 2–3 цвета из палитры для гармоничных образов.\",\n    \"Используй нейтральные оттенки как основу своего гардероба.\",\n    \"Добавляй акцентные цвета в аксессуарах и деталях.\"\n  ];\n  rules.forEach((r,i)=>{\n    const cy=L.usage.rowY+i*L.usage.rowHeight;\n    o+=usageIcon(i,L.usage.iconX+14,cy-5,t);\n    o+=wrapped({value:r,x:L.usage.textX,y:cy-10,maxChars:42,maxLines:2,lineHeight:13,size:9.3,family:\"Georgia, 'Times New Roman', serif\"});\n  });\n\n  o+=panel(L.important,\"#F8E9DD\",t.line,L.important.radius);\n  o+=text({value:\"✦\",x:L.important.starX,y:L.important.starY,size:25,fill:t.accent,anchor:\"middle\"});\n  o+=text({value:\"Важно\",x:L.important.titleX,y:L.important.titleY,size:18,weight:700,family:\"Georgia, 'Times New Roman', serif\"});\n  const note=data.important_note||\"Не обязательно использовать все оттенки одновременно — выбирай те, в которых ты чувствуешь себя гармонично и уверенно.\";\n  o+=wrapped({value:note,x:L.important.textX,y:L.important.textY,maxChars:30,maxLines:7,lineHeight:L.important.lineHeight,size:10.5,family:\"Georgia, 'Times New Roman', serif\"});\n  o+=`<path d=\"M ${L.important.branchX} ${L.important.branchY+L.important.branchHeight} C ${L.important.branchX+10} ${L.important.branchY+115}, ${L.important.branchX+70} ${L.important.branchY+65}, ${L.important.branchX+L.important.branchWidth} ${L.important.branchY}\" fill=\"none\" stroke=\"${t.accent}\" stroke-opacity=\".38\" stroke-width=\"2\"/>`;\n  for(let i=0;i<8;i++){const x=L.important.branchX+20+i*13,y=L.important.branchY+L.important.branchHeight-25-i*19;o+=`<ellipse cx=\"${x}\" cy=\"${y}\" rx=\"12\" ry=\"4\" transform=\"rotate(${-35+i*7} ${x} ${y})\" fill=\"${t.accent}\" fill-opacity=\".20\"/>`;}\n\n  o+=`<line x1=\"${L.footer.x}\" y1=\"${L.footer.y}\" x2=\"${L.footer.x+L.footer.width}\" y2=\"${L.footer.y}\" stroke=\"${t.line}\"/>`;\n  o+=drawLogo({href:obj(data.assets).logo,x:L.footer.logoX,y:L.footer.logoY,width:L.footer.logoWidth,height:L.footer.logoHeight});\n  o+=text({value:\"✦\",x:L.footer.leftStarX,y:L.footer.sloganY-2,size:14,fill:t.accent,anchor:\"middle\"});\n  o+=text({value:\"✦\",x:L.footer.rightStarX,y:L.footer.sloganY-2,size:14,fill:t.accent,anchor:\"middle\"});\n  o+=text({value:\"Ц В Е Т   У К Р А Ш А Е Т   Т Е Б Я\",x:L.footer.x+L.footer.width/2,y:L.footer.sloganY,size:9,weight:600,family:\"Georgia, 'Times New Roman', serif\",anchor:\"middle\",tracking:2});\n  o+=svg.end();\n  return o;\n}\nmodule.exports={PAGE_2_VERSION,buildPage2};\n", "passport/passport_builder.js": "/**\n * DiArt Passport\n * File: passport/passport_builder.js\n * Version: 3.1.0\n *\n * Final entry point.\n * Accepts either:\n * - normalized passport object\n * - wrapper from passport_input_adapter.js\n *\n * Returns:\n * - two SVG pages\n * - file names\n * - passport metadata\n * - warnings\n */\n\n\"use strict\";\n\nconst { buildPage1 } = require(\"./page1\");\nconst { buildPage2 } = require(\"./page2\");\n\nconst PASSPORT_BUILDER_VERSION = \"3.1.0\";\n\nfunction isObject(value) {\n  return value !== null && typeof value === \"object\" && !Array.isArray(value);\n}\n\nfunction unwrapInput(input) {\n  if (\n    isObject(input) &&\n    input.ok === true &&\n    input.stage === \"passport_input_normalized\" &&\n    isObject(input.result)\n  ) {\n    return input.result;\n  }\n\n  if (isObject(input) && isObject(input.result) && input.result.season) {\n    return input.result;\n  }\n\n  if (isObject(input) && input.season) {\n    return input;\n  }\n\n  throw new Error(\n    \"Passport Builder: не удалось распознать входные данные.\"\n  );\n}\n\nfunction sanitizeFilePart(value, fallback) {\n  const result = String(value || fallback || \"passport\")\n    .trim()\n    .toLowerCase()\n    .replace(/[^a-z0-9а-яё_-]+/gi, \"_\")\n    .replace(/_+/g, \"_\")\n    .replace(/^_+|_+$/g, \"\");\n\n  return result || fallback || \"passport\";\n}\n\nfunction validate(data) {\n  if (!isObject(data)) {\n    throw new Error(\"Passport Builder: data is missing.\");\n  }\n\n  if (!isObject(data.season) || !data.season.id) {\n    throw new Error(\"Passport Builder: season.id is missing.\");\n  }\n\n  if (!isObject(data.confidence)) {\n    throw new Error(\"Passport Builder: confidence is missing.\");\n  }\n\n  if (!isObject(data.scales)) {\n    throw new Error(\"Passport Builder: scales are missing.\");\n  }\n\n  if (!Array.isArray(data.top3) || data.top3.length < 3) {\n    throw new Error(\"Passport Builder: top3 must contain 3 seasons.\");\n  }\n\n  if (!isObject(data.palette)) {\n    throw new Error(\"Passport Builder: palette is missing.\");\n  }\n\n  if (\n    !isObject(data.assets) ||\n    !isObject(data.assets.pin_manifest)\n  ) {\n    throw new Error(\n      \"Passport Builder: assets.pin_manifest is missing.\"\n    );\n  }\n}\n\nfunction buildPassport(input) {\n  try {\n    const data = unwrapInput(input);\n    validate(data);\n\n    const page1Svg = buildPage1(data);\n    const page2Svg = buildPage2(data);\n\n    const passportId = sanitizeFilePart(\n      data.passport && data.passport.id,\n      \"diart_passport\"\n    );\n\n    const seasonId = sanitizeFilePart(\n      data.season.id,\n      \"season\"\n    );\n\n    return {\n      ok: true,\n      stage: \"passport_built\",\n      builder_version: PASSPORT_BUILDER_VERSION,\n\n      passport_id:\n        data.passport && data.passport.id\n          ? data.passport.id\n          : null,\n\n      season_id: data.season.id,\n\n      confidence_percent:\n        Number.isFinite(Number(data.confidence.percent))\n          ? Number(data.confidence.percent)\n          : null,\n\n      page_1_svg: page1Svg,\n      page_2_svg: page2Svg,\n\n      page_1_filename:\n        `${passportId}_${seasonId}_page_1.svg`,\n\n      page_2_filename:\n        `${passportId}_${seasonId}_page_2.svg`,\n\n      warnings:\n        Array.isArray(data.warnings)\n          ? data.warnings\n          : []\n    };\n  } catch (error) {\n    return {\n      ok: false,\n      stage: \"passport_builder_failed\",\n      builder_version: PASSPORT_BUILDER_VERSION,\n      error: {\n        message:\n          error && error.message\n            ? error.message\n            : \"Неизвестная ошибка Passport Builder.\"\n      }\n    };\n  }\n}\n\nmodule.exports = {\n  PASSPORT_BUILDER_VERSION,\n  buildPassport\n};\n", "passport/palette_resolver.js": "/**\n * DiArt Passport\n * File: passport/palette_resolver.js\n * Version: 1.0.0\n *\n * Resolves one seasonal palette from:\n * - DiArt Palette Database v2.1\n * - DiArt Master Color Research Library v3.9\n *\n * Palette Database defines:\n * - season\n * - palette order\n * - group assignment\n *\n * Master Color Library defines:\n * - canonical HEX\n * - RGB / LAB / LCH\n * - research metadata\n *\n * The resolver does not invent Russian color names.\n */\n\n\"use strict\";\n\nconst RESOLVER_VERSION = \"1.0.0\";\n\nconst EXPECTED_GROUP_COUNTS = Object.freeze({\n  signature: 4,\n  core: 4,\n  additional: 4,\n  neutral: 6,\n  accent: 6\n});\n\nfunction isObject(value) {\n  return value !== null && typeof value === \"object\" && !Array.isArray(value);\n}\n\nfunction fail(message, details) {\n  const error = new Error(message);\n  error.details = details || null;\n  throw error;\n}\n\nfunction parseMaybeJson(value, fieldName) {\n  if (isObject(value) || Array.isArray(value)) {\n    return value;\n  }\n\n  if (typeof value !== \"string\" || !value.trim()) {\n    fail(`Поле \"${fieldName}\" не передано.`);\n  }\n\n  try {\n    return JSON.parse(value);\n  } catch (error) {\n    fail(`Не удалось разобрать JSON в поле \"${fieldName}\".`, {\n      field: fieldName,\n      parser_message: error.message\n    });\n  }\n}\n\nfunction normalizeHex(value, fallback = \"#CCCCCC\") {\n  if (typeof value !== \"string\") return fallback;\n\n  const hex = value.trim().toUpperCase();\n\n  if (/^#[0-9A-F]{6}$/.test(hex)) return hex;\n  if (/^[0-9A-F]{6}$/.test(hex)) return `#${hex}`;\n\n  return fallback;\n}\n\nfunction makeMasterIndex(masterLibrary) {\n  const colors = Array.isArray(masterLibrary.colors)\n    ? masterLibrary.colors\n    : [];\n\n  const byColorId = {};\n  const bySlug = {};\n\n  for (const color of colors) {\n    if (!isObject(color)) continue;\n\n    if (color.color_id) {\n      byColorId[String(color.color_id)] = color;\n    }\n\n    if (color.slug) {\n      bySlug[String(color.slug)] = color;\n    }\n  }\n\n  return {\n    byColorId,\n    bySlug,\n    count: colors.length\n  };\n}\n\nfunction findSeason(paletteDatabase, seasonId) {\n  const seasons = Array.isArray(paletteDatabase.seasons)\n    ? paletteDatabase.seasons\n    : [];\n\n  return (\n    seasons.find(season => season && season.id === seasonId) ||\n    null\n  );\n}\n\nfunction getMasterColor(entry, index) {\n  if (!entry) return null;\n\n  if (entry.color_id && index.byColorId[entry.color_id]) {\n    return index.byColorId[entry.color_id];\n  }\n\n  if (entry.slug && index.bySlug[entry.slug]) {\n    return index.bySlug[entry.slug];\n  }\n\n  return null;\n}\n\nfunction resolveName(entry, masterColor) {\n  const entryName = isObject(entry.name) ? entry.name : {};\n  const masterName =\n    masterColor && isObject(masterColor.name)\n      ? masterColor.name\n      : {};\n\n  return {\n    ru:\n      entryName.ru ||\n      masterName.ru ||\n      null,\n    en:\n      entryName.en ||\n      masterName.en ||\n      entry.slug ||\n      \"Unnamed color\"\n  };\n}\n\nfunction resolveCanonicalHex(entry, masterColor) {\n  const masterHex =\n    masterColor &&\n    masterColor.research &&\n    masterColor.research.canonical_hex;\n\n  return normalizeHex(\n    masterHex ||\n    entry.canonical_hex ||\n    \"#CCCCCC\"\n  );\n}\n\nfunction resolveComputed(entry, masterColor) {\n  if (\n    masterColor &&\n    isObject(masterColor.computed)\n  ) {\n    return {\n      rgb: masterColor.computed.rgb || entry.rgb || null,\n      lab: masterColor.computed.lab || entry.lab || null,\n      lch: masterColor.computed.lch || entry.lch || null\n    };\n  }\n\n  return {\n    rgb: entry.rgb || null,\n    lab: entry.lab || null,\n    lch: entry.lch || null\n  };\n}\n\nfunction resolveResearch(entry, masterColor) {\n  const research =\n    masterColor && isObject(masterColor.research)\n      ? masterColor.research\n      : {};\n\n  return {\n    status:\n      research.status ||\n      entry.research_status ||\n      null,\n    agreement:\n      research.agreement ||\n      entry.agreement ||\n      null,\n    hex_source:\n      research.hex_source ||\n      entry.hex_source ||\n      null,\n    canonical_hex_reason:\n      research.canonical_hex_reason ||\n      null\n  };\n}\n\nfunction resolveEntry(entry, index, warnings) {\n  const masterColor = getMasterColor(entry, index);\n\n  if (!masterColor) {\n    warnings.push(\n      `Master Color Library: не найден цвет ${entry.color_id || entry.slug || entry.palette_entry_id}.`\n    );\n  }\n\n  const name = resolveName(entry, masterColor);\n  const computed = resolveComputed(entry, masterColor);\n  const research = resolveResearch(entry, masterColor);\n\n  if (!name.ru) {\n    warnings.push(\n      `Отсутствует русское название цвета ${entry.color_id || entry.slug || entry.palette_entry_id}.`\n    );\n  }\n\n  return {\n    palette_entry_id: entry.palette_entry_id || null,\n    color_id:\n      entry.color_id ||\n      (masterColor && masterColor.color_id) ||\n      null,\n    slug:\n      entry.slug ||\n      (masterColor && masterColor.slug) ||\n      null,\n    group: entry.group || null,\n    order: null,\n\n    name_ru: name.ru,\n    name_en: name.en,\n\n    hex: resolveCanonicalHex(entry, masterColor),\n\n    rgb: computed.rgb,\n    lab: computed.lab,\n    lch: computed.lch,\n\n    research\n  };\n}\n\nfunction groupColors(colors) {\n  const groups = {\n    signature: [],\n    core: [],\n    additional: [],\n    neutral: [],\n    accent: []\n  };\n\n  for (const color of colors) {\n    if (groups[color.group]) {\n      groups[color.group].push(color);\n    }\n  }\n\n  return groups;\n}\n\nfunction validateGroupCounts(groups, warnings) {\n  for (const [group, expected] of Object.entries(\n    EXPECTED_GROUP_COUNTS\n  )) {\n    const actual = Array.isArray(groups[group])\n      ? groups[group].length\n      : 0;\n\n    if (actual !== expected) {\n      warnings.push(\n        `Группа \"${group}\" содержит ${actual} цветов вместо ${expected}.`\n      );\n    }\n  }\n}\n\nfunction resolvePalette({\n  paletteDatabase,\n  masterColorLibrary,\n  seasonId\n}) {\n  try {\n    const paletteDb = parseMaybeJson(\n      paletteDatabase,\n      \"paletteDatabase\"\n    );\n\n    const masterDb = parseMaybeJson(\n      masterColorLibrary,\n      \"masterColorLibrary\"\n    );\n\n    if (!seasonId || typeof seasonId !== \"string\") {\n      fail(\"Не указан seasonId.\");\n    }\n\n    const season = findSeason(paletteDb, seasonId);\n\n    if (!season) {\n      fail(`В Palette Database не найден сезон \"${seasonId}\".`);\n    }\n\n    const masterIndex = makeMasterIndex(masterDb);\n    const warnings = [];\n\n    const sourceColors = Array.isArray(season.colors)\n      ? season.colors\n      : [];\n\n    const colors = sourceColors.map((entry, index) => {\n      const resolved = resolveEntry(entry, masterIndex, warnings);\n      resolved.order = index + 1;\n      return resolved;\n    });\n\n    const groups = groupColors(colors);\n    validateGroupCounts(groups, warnings);\n\n    const missingRussianNames = colors.filter(\n      color => !color.name_ru\n    ).length;\n\n    return {\n      ok: true,\n      stage: \"palette_resolved\",\n      resolver_version: RESOLVER_VERSION,\n\n      metadata: {\n        season_id: seasonId,\n        season_name: season.name || null,\n        palette_database_version:\n          paletteDb.metadata &&\n          paletteDb.metadata.version\n            ? String(paletteDb.metadata.version)\n            : null,\n        master_color_version:\n          masterDb.metadata &&\n          masterDb.metadata.version\n            ? String(masterDb.metadata.version)\n            : null,\n        total_colors: colors.length,\n        master_indexed_colors: masterIndex.count,\n        missing_russian_names: missingRussianNames\n      },\n\n      palette: {\n        season_id: seasonId,\n        name: season.name || null,\n        colors,\n        groups\n      },\n\n      warnings\n    };\n  } catch (error) {\n    return {\n      ok: false,\n      stage: \"palette_resolver_failed\",\n      resolver_version: RESOLVER_VERSION,\n      error: {\n        message:\n          error && error.message\n            ? error.message\n            : \"Неизвестная ошибка Palette Resolver.\",\n        details:\n          error && error.details\n            ? error.details\n            : null\n      }\n    };\n  }\n}\n\nmodule.exports = {\n  RESOLVER_VERSION,\n  resolvePalette\n};\n", "passport/passport_input_adapter.js": "/**\n * DiArt Passport\n * File: passport/passport_input_adapter.js\n * Version: 1.1.0\n */\n\n\"use strict\";\n\nconst ADAPTER_VERSION = \"1.1.0\";\n\nconst SEASONS = {\n  light_spring: [\"Светлая Весна\", \"Light Spring\", \"Светлый, тёплый и свежий природный колорит\"],\n  true_spring: [\"Тёплая Весна\", \"True Spring\", \"Тёплый, ясный и золотистый природный колорит\"],\n  bright_spring: [\"Яркая Весна\", \"Bright Spring\", \"Яркий, тёплый и контрастный природный колорит\"],\n  light_summer: [\"Светлое Лето\", \"Light Summer\", \"Светлый, прохладный и мягкий природный колорит\"],\n  true_summer: [\"Истинное Лето\", \"True Summer\", \"Прохладный, спокойный и гармоничный природный колорит\"],\n  soft_summer: [\"Мягкое Лето\", \"Soft Summer\", \"Мягкий, прохладный и приглушённый природный колорит\"],\n  soft_autumn: [\"Мягкая Осень\", \"Soft Autumn\", \"Мягкий, тёплый и приглушённый природный колорит\"],\n  true_autumn: [\"Истинная Осень\", \"True Autumn\", \"Тёплый, насыщенный и землистый природный колорит\"],\n  deep_autumn: [\"Глубокая Осень\", \"Deep Autumn\", \"Тёплый, глубокий и насыщенный природный колорит\"],\n  bright_winter: [\"Яркая Зима\", \"Bright Winter\", \"Яркий, прохладный и контрастный природный колорит\"],\n  true_winter: [\"Истинная Зима\", \"True Winter\", \"Прохладный, чистый и контрастный природный колорит\"],\n  deep_winter: [\"Глубокая Зима\", \"Deep Winter\", \"Прохладный, глубокий и контрастный природный колорит\"]\n};\n\nfunction obj(v){return v&&typeof v===\"object\"&&!Array.isArray(v)?v:{};}\nfunction arr(v){return Array.isArray(v)?v:[];}\nfunction parse(v,name){\n  if(v===undefined||v===null||v===\"\") return null;\n  if(typeof v===\"object\") return v;\n  try{return JSON.parse(v);}catch(e){throw new Error(`Некорректный JSON: ${name}`);}\n}\nfunction unwrapEngine(v){\n  if(Array.isArray(v)) v=v[0];\n  if(obj(v).result&&obj(v.result).runtime_version) return v.result;\n  return v;\n}\nfunction clamp(v,min,max){\n  const n=Number(v);\n  return Number.isFinite(n)?Math.min(max,Math.max(min,n)):min;\n}\nfunction round(v,d=3){\n  const p=Math.pow(10,d);\n  return Math.round((Number(v)+Number.EPSILON)*p)/p;\n}\nfunction hex(v,fallback=\"#CCCCCC\"){\n  if(typeof v!==\"string\") return fallback;\n  const x=v.trim().toUpperCase();\n  if(/^#[0-9A-F]{6}$/.test(x)) return x;\n  if(/^[0-9A-F]{6}$/.test(x)) return \"#\"+x;\n  return fallback;\n}\nfunction weighted(scores,weights){\n  scores=obj(scores);\n  let a=0,b=0;\n  for(const [k,w] of Object.entries(weights)){\n    const s=Number(scores[k]);\n    if(Number.isFinite(s)&&s>=0){a+=s*w;b+=s;}\n  }\n  return b?clamp(a/b,0,1):0.5;\n}\nfunction labelConfidence(p){\n  if(p>=90)return\"Очень высокая достоверность\";\n  if(p>=75)return\"Высокая достоверность\";\n  if(p>=60)return\"Вероятное совпадение\";\n  return\"Предварительный результат\";\n}\nfunction scaleLabel(type,value){\n  const map={\n    temperature:{cool:\"Холодная\",neutral_cool:\"Нейтрально-холодная\",neutral:\"Нейтральная\",neutral_warm:\"Нейтрально-тёплая\",warm:\"Тёплая\"},\n    depth:{light:\"Светлая\",light_medium:\"Светло-средняя\",medium:\"Средняя\",medium_deep:\"Средне-глубокая\",deep:\"Глубокая\",very_dark:\"Очень глубокая\",very_deep:\"Очень глубокая\"},\n    clarity:{muted:\"Приглушённая\",soft:\"Мягкая\",balanced:\"Сбалансированная\",clear:\"Чистая\",bright:\"Яркая\",moderate:\"Сбалансированная\"},\n    contrast:{low:\"Низкий\",medium:\"Средний\",high:\"Высокий\",moderate:\"Средний\"}\n  };\n  return map[type]&&map[type][value]?map[type][value]:String(value||\"\").replace(/_/g,\" \");\n}\nfunction naturalName(value,fallback){\n  const map={black:\"Чёрный\",dark_brown:\"Тёмно-коричневый\",medium_brown:\"Коричневый\",light_brown:\"Светло-коричневый\",hazel:\"Ореховый\",amber:\"Янтарный\",green:\"Зелёный\",blue:\"Голубой\",gray:\"Серый\"};\n  return map[String(value||\"\").toLowerCase()]||fallback;\n}\nfunction formatDate(v){\n  if(typeof v===\"string\"&&/^\\d{2}\\.\\d{2}\\.\\d{4}$/.test(v)) return v;\n  const d=v?new Date(v):new Date();\n  return `${String(d.getDate()).padStart(2,\"0\")}.${String(d.getMonth()+1).padStart(2,\"0\")}.${d.getFullYear()}`;\n}\nfunction passportId(v,season){\n  if(v&&String(v).trim()) return String(v).trim();\n  const code=season.split(\"_\").map(x=>x[0]).join(\"\").toUpperCase();\n  return `DIART-${code}-${Date.now().toString(36).toUpperCase()}`;\n}\nfunction top3(engine){\n  return arr(engine.season_ranking)\n    .filter(x=>x&&!x.hard_excluded)\n    .sort((a,b)=>Number(a.rank||999)-Number(b.rank||999))\n    .slice(0,3)\n    .map((x,i)=>{\n      const s=SEASONS[x.season_id];\n      if(!s) throw new Error(`Неизвестный сезон: ${x.season_id}`);\n      return {\n        rank:i+1,\n        season_id:x.season_id,\n        name_ru:s[0],\n        name_en:s[1],\n        score:round(x.score_after_modifiers!==undefined?x.score_after_modifiers:x.base_score,1)\n      };\n    });\n}\nfunction samples(raw,natural){\n  raw=obj(raw);\n  function one(key){\n    const s=obj(raw[key]);\n    return {\n      url:typeof s.url===\"string\"?s.url:\"\",\n      name_ru:s.name_ru||natural[key].name_ru,\n      hex:hex(s.hex,natural[key].hex),\n      swatches:arr(s.swatches).map(x=>hex(x,null)).filter(Boolean).slice(0,6)\n    };\n  }\n  return {eye:one(\"eye\"),hair:one(\"hair\"),skin:one(\"skin\")};\n}\nfunction main(input){\n  try{\n    input=obj(input);\n\n    const engine=unwrapEngine(parse(input.engineResult,\"engineResult\"));\n    const paletteResult=parse(input.paletteResult,\"paletteResult\");\n    const pinManifest=parse(input.pinManifest,\"pinManifest\");\n    const photoSamples=parse(input.photoSamples,\"photoSamples\")||{};\n\n    if(!obj(engine).result||!engine.result.best_match) throw new Error(\"Некорректный output Color Engine.\");\n    if(!obj(paletteResult).palette) throw new Error(\"Некорректный output Palette Resolver.\");\n\n    const seasonId=String(engine.result.best_match);\n    const seasonData=SEASONS[seasonId];\n    if(!seasonData) throw new Error(`Неизвестный сезон: ${seasonId}`);\n\n    const dims=obj(engine.dimension_results);\n    const observed=obj(engine.observed_colors);\n    const skin=obj(observed.skin),eyes=obj(observed.eyes),hair=obj(observed.hair);\n\n    const natural={\n      eye:{hex:hex(eyes.hex,\"#777777\"),name_ru:naturalName(eyes.primary_color,\"Оттенок глаз\")},\n      hair:{hex:hex(hair.hex,\"#555555\"),name_ru:naturalName(hair.primary_color,\"Оттенок волос\")},\n      skin:{hex:hex(skin.hex,\"#D8B49A\"),name_ru:skin.surface_tone||\"Оттенок кожи\"}\n    };\n\n    const confidencePercent=Math.round(clamp(engine.result.confidence_percent,0,100));\n    const meta=obj(paletteResult.metadata);\n    const warnings=[\n      ...arr(paletteResult.warnings),\n      ...(!input.photoUrl?[\"photoUrl не передан.\"]:[]),\n      ...(!input.logoUrl?[\"logoUrl не передан.\"]:[]),\n      ...(!pinManifest?[\"pinManifest не передан.\"]:[])\n    ];\n\n    return {\n      ok:true,\n      stage:\"passport_input_normalized\",\n      adapter_version:ADAPTER_VERSION,\n      result:{\n        adapter:{name:\"DiArt Passport Input Adapter\",version:ADAPTER_VERSION},\n        passport:{\n          id:passportId(input.passportId,seasonId),\n          created_at:formatDate(input.createdAt),\n          client_name:typeof input.clientName===\"string\"?input.clientName.trim():\"\",\n          ai_model:typeof input.aiModel===\"string\"&&input.aiModel.trim()?input.aiModel.trim():\"Не указана\",\n          engine_runtime_version:String(engine.runtime_version||\"\"),\n          extractor_version:engine.extractor&&engine.extractor.version?String(engine.extractor.version):\"\",\n          database_version:meta.palette_database_version||\"\",\n          master_color_version:meta.master_color_version||\"\"\n        },\n        source:{photo_url:typeof input.photoUrl===\"string\"?input.photoUrl.trim():\"\"},\n        quality:{\n          overall_quality:engine.quality&&engine.quality.overall_quality||null,\n          continue_analysis:engine.quality&&engine.quality.continue_analysis,\n          problems:engine.quality&&arr(engine.quality.problems),\n          limitations:engine.quality&&arr(engine.quality.limitations)\n        },\n        season:{id:seasonId,name_ru:seasonData[0],name_en:seasonData[1],description_ru:seasonData[2]},\n        confidence:{\n          value:round(clamp(engine.result.confidence,0,1)),\n          percent:confidencePercent,\n          level:String(engine.result.confidence_level||\"\"),\n          label_ru:labelConfidence(confidencePercent),\n          decision_status:String(engine.result.decision_status||\"\"),\n          score_gap_to_second:round(clamp(engine.result.score_gap_to_second,0,100),1)\n        },\n        scales:{\n          temperature:{\n            classification:dims.temperature.classification,\n            label_ru:scaleLabel(\"temperature\",dims.temperature.classification),\n            position:round(weighted(dims.temperature.scores,{cool:0,neutral:0.5,warm:1}))\n          },\n          depth:{\n            classification:dims.value.classification,\n            label_ru:scaleLabel(\"depth\",dims.value.classification),\n            position:round(weighted(dims.value.scores,{light:0,medium:0.5,deep:1}))\n          },\n          clarity:{\n            classification:dims.chroma.classification,\n            label_ru:scaleLabel(\"clarity\",dims.chroma.classification),\n            position:round(weighted(dims.chroma.scores,{muted:0,soft:0.25,balanced:0.5,clear:0.75,bright:1}))\n          },\n          contrast:{\n            classification:dims.contrast.classification,\n            label_ru:scaleLabel(\"contrast\",dims.contrast.classification),\n            position:round(weighted(dims.contrast.scores,{low:0,medium:0.5,high:1}))\n          }\n        },\n        top3:top3(engine),\n        natural_colors:natural,\n        photo_samples:samples(photoSamples,natural),\n        palette:paletteResult.palette,\n        assets:{\n          logo:typeof input.logoUrl===\"string\"?input.logoUrl.trim():\"\",\n          pin_manifest:pinManifest\n        },\n        harmony_guide:arr(input.harmonyGuide),\n        important_note:typeof input.importantNote===\"string\"?input.importantNote:\"\",\n        warnings\n      }\n    };\n  }catch(error){\n    return {\n      ok:false,\n      stage:\"passport_input_adapter_failed\",\n      adapter_version:ADAPTER_VERSION,\n      error:{message:error&&error.message?error.message:\"Неизвестная ошибка Input Adapter.\"}\n    };\n  }\n}\n\nmodule.exports={ADAPTER_VERSION,main};\n"};

function isObject(value) {
  return value !== null && typeof value === "object" && !Array.isArray(value);
}
function parseMaybeJson(value, fieldName, fallback = null) {
  if (value === undefined || value === null || value === "") return fallback;
  if (isObject(value) || Array.isArray(value)) return value;
  if (typeof value !== "string") throw new Error(`Поле "${fieldName}" должно быть объектом или JSON-строкой.`);
  try { return JSON.parse(value); }
  catch (error) { throw new Error(`Не удалось разобрать JSON в поле "${fieldName}": ${error.message}`); }
}
function normalizePath(path) {
  const parts = [];
  String(path || "").replace(/\\/g, "/").split("/").forEach(part => {
    if (!part || part === ".") return;
    if (part === "..") { parts.pop(); return; }
    parts.push(part);
  });
  let result = parts.join("/");
  if (!result.endsWith(".js")) result += ".js";
  return result;
}
function dirname(path) {
  const parts = normalizePath(path).split("/");
  parts.pop();
  return parts.join("/");
}
function resolveRequest(fromId, request) {
  const raw = String(request || "").trim();
  if (!raw) throw new Error(`Пустой require() в модуле "${fromId}".`);
  return raw.startsWith(".")
    ? normalizePath(`${dirname(fromId)}/${raw}`)
    : normalizePath(raw);
}
function createModuleSystem(sourceMap) {
  const cache = {};
  function load(moduleId) {
    const id = normalizePath(moduleId);
    if (cache[id]) return cache[id].exports;
    const source = sourceMap[id];
    if (typeof source !== "string" || !source.trim()) throw new Error(`Исходный код модуля "${id}" не найден.`);
    const module = { id, exports: {} };
    cache[id] = module;
    function localRequire(request) { return load(resolveRequest(id, request)); }
    try {
      const execute = new Function("module", "exports", "require", `"use strict";\n${source}\n//# sourceURL=${id}`);
      execute(module, module.exports, localRequire);
    } catch (error) {
      delete cache[id];
      throw new Error(`Ошибка выполнения модуля "${id}": ${error.message}`);
    }
    return module.exports;
  }
  return { load };
}
function unwrapEngineForSeason(value) {
  let engine = value;
  if (Array.isArray(engine)) engine = engine[0];
  if (isObject(engine) && isObject(engine.result) && engine.result.runtime_version) engine = engine.result;
  if (isObject(engine) && isObject(engine.result) && engine.result.best_match) return engine.result.best_match;
  throw new Error("Не удалось получить best_match из Color Engine.");
}
function runPipeline(input) {
  const engineResult = parseMaybeJson(input.engineResult, "engineResult");
  const paletteDatabase = parseMaybeJson(input.paletteDatabase, "paletteDatabase");
  const masterColorLibrary = parseMaybeJson(input.masterColorLibrary, "masterColorLibrary");
  const pinManifest = parseMaybeJson(input.pinManifest, "pinManifest");
  const photoSamples = parseMaybeJson(input.photoSamples, "photoSamples", {});

  const modules = createModuleSystem(EMBEDDED_SOURCES);
  const paletteResolver = modules.load("passport/palette_resolver.js");
  const adapter = modules.load("passport/passport_input_adapter.js");
  const builder = modules.load("passport/passport_builder.js");

  const seasonId = unwrapEngineForSeason(engineResult);
  const paletteResult = paletteResolver.resolvePalette({ paletteDatabase, masterColorLibrary, seasonId });
  if (!paletteResult || paletteResult.ok !== true) {
    throw new Error(paletteResult && paletteResult.error && paletteResult.error.message
      ? paletteResult.error.message : "Palette Resolver завершился с ошибкой.");
  }

  const adapterResult = adapter.main({
    engineResult, paletteResult, pinManifest,
    photoUrl: input.photoUrl,
    logoUrl: input.logoUrl,
    photoSamples,
    aiModel: input.aiModel,
    clientName: input.clientName,
    createdAt: input.createdAt,
    passportId: input.passportId,
    harmonyGuide: input.harmonyGuide,
    importantNote: input.importantNote
  });
  if (!adapterResult || adapterResult.ok !== true) {
    throw new Error(adapterResult && adapterResult.error && adapterResult.error.message
      ? adapterResult.error.message : "Passport Input Adapter завершился с ошибкой.");
  }

  const result = builder.buildPassport(adapterResult);
  if (!result || result.ok !== true) {
    throw new Error(result && result.error && result.error.message
      ? result.error.message : "Passport Builder завершился с ошибкой.");
  }

  return {
    ok: true,
    stage: "diart_passport_bundle_completed",
    bundle_version: BUNDLE_VERSION,
    ...result
  };
}
function main(input) {
  try {
    if (!isObject(input)) throw new Error("Make Code не передал объект input.");
    return runPipeline(input);
  } catch (error) {
    return {
      ok: false,
      stage: "diart_passport_bundle_failed",
      bundle_version: BUNDLE_VERSION,
      error: { message: error && error.message ? error.message : "Неизвестная ошибка DiArt Passport Bundle." }
    };
  }
}
return main(input);
